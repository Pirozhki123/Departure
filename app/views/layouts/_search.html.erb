<% if customer_signed_in? %>
  <div class="search_form mb-2">
    <%= form_with url: public_search_path, local: true, method: :get do |f| %>
      <%= f.text_field :word %>
      <%= f.select :range, options_for_select([['Word'], ['User']]) %>
      <%= f.select :search, options_for_select([["完全一致","perfect_match"], ["前方一致","forward_match"], ["後方一致","backward_match"], ["部分一致","partial_match"]]) %>
      <%= f.submit "検索", class: "btn btn-primary" %>
    <% end %>
  </div>
  <div class="search_form mb-2">
    <%= form_with url: public_search_path, local: true, method: :get do |f| %>
      <%= f.text_field :word %>
      <%= f.hidden_field :range, :value => "tag" %>
      <%= f.hidden_field :search, :value => "perfect_match" %><br>
      <%= f.submit "タグ検索", class: "btn btn-primary" %>
    <% end %>
  </div>
<% end %>